# Wedding Website — 설계 문서

> 파일: `index.html`  
> 배포: https://ywctak.github.io/wedding/  
> 최종 수정: 2026-07-22

---

## 1. 개요

단일 HTML 파일로 구성된 정적 웨딩 초대 웹사이트.  
외부 프레임워크 없이 순수 HTML / CSS / Vanilla JS로 작성되어 GitHub Pages에서 즉시 서빙 가능.

---

## 2. 파일 구조

```
wedding/
├── index.html   # 전체 사이트 (단일 파일)
└── index.md     # 설계 문서 (현재 파일)
```

---

## 3. 디자인 시스템

### 3.1 컬러 팔레트

| 변수명      | 값          | 용도                        |
|-------------|-------------|-----------------------------|
| `--gold`    | `#c9a96e`   | 주 강조색 (텍스트, 테두리)  |
| `--cream`   | `#faf6f0`   | 기본 본문 텍스트 색         |
| `--dark`    | `#1a1a2e`   | 배경 (딥 네이비)            |

### 3.2 타이포그래피

| 폰트                  | 출처           | 용도                              |
|-----------------------|----------------|-----------------------------------|
| Cormorant Garamond    | Google Fonts   | 모노그램, 섹션 제목, 날짜, 타임라인 |
| Montserrat            | Google Fonts   | 본문, 버튼, 레이블                |

- 기본 font-weight: **300** (Light)
- 반응형 폰트 크기: `clamp()` 사용으로 모바일~데스크탑 자동 조정

### 3.3 모바일 정책

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0,
      maximum-scale=1.0, user-scalable=no" />
```

- 핀치 줌 비활성화 (`user-scalable=no`, `maximum-scale=1.0`)
- iOS Safari는 접근성 정책으로 일부 무시할 수 있음

---

## 4. 섹션 구성

```
[Canvas — 파티클 배경 (fixed, z-index: 0)]
│
├── § Hero           — 전체 화면 랜딩
├── § Countdown      — 디데이 카운트다운
├── § Day of Events  — 타임라인
└── § Location       — 오시는 길
```

---

## 5. 섹션별 상세

### 5.1 Hero (`section.hero`)

| 요소             | 클래스/ID        | 내용                                            |
|------------------|------------------|-------------------------------------------------|
| 모노그램         | `.monogram`      | `A & B` (이탤릭 `&` — `.ampersand`)            |
| 태그라인         | `.tagline`       | `영원히 함께`                                   |
| 구분선           | `.divider`       | 골드 라인 + 다이아몬드 아이콘 (`◆`)             |
| 날짜             | `.date-text`     | `2026년 11월 7일`                               |
| 장소             | `.location`      | `압구정 소망교회 2층 선교관`                    |
| CTA 버튼         | `.cta > .btn`    | 참석 여부 회신 / 우리의 이야기 (href="#" 더미)  |

**입장 애니메이션 순서 (staggered):**

| 요소          | 애니메이션  | 딜레이  |
|---------------|-------------|---------|
| `.monogram`   | fadeDown    | 0s      |
| `.tagline`    | fadeDown    | 0.2s    |
| `.divider`    | fadeIn      | 0.5s    |
| `.date-text`  | fadeIn      | 0.7s    |
| `.location`   | fadeIn      | 0.9s    |
| `.cta`        | fadeIn      | 1.1s    |

### 5.2 Countdown (`section.countdown-section`)

- 목표 시각: `2026-11-07T14:00:00` (오후 2시, 로컬 시간 기준)
- `setInterval` 1000ms 주기로 갱신
- 표시 단위: 일(3자리) / 시간 / 분 / 초 (2자리, 0-padding)

### 5.3 Day of Events (`section.timeline-section`)

중앙 수직 골드 라인 기준 좌우 교대 배치. 시작 시각 오후 2시 기준.

| 시간        | 이벤트                  | 위치  |
|-------------|-------------------------|-------|
| 오후 2:00   | 하객 입장 및 웰컴 드링크 | 좌    |
| 오후 3:00   | 결혼 예식               | 우    |
| 오후 4:00   | 칵테일 타임             | 좌    |
| 오후 6:00   | 피로연 및 축하 만찬     | 우    |
| 오후 10:00  | 피날레                  | 좌    |

### 5.4 Location (`section.location-section`)

오시는 길 섹션. 구성 순서:

```
[섹션 레이블] LOCATION
[섹션 제목]   오시는 길

[주소 블록]
  서울특별시 강남구 압구정로36길 55
  압구정 소망교회 2층 선교관

[구분선]

[지도 — 네이버 지도 (소망교회)]
  → Naver Maps JavaScript API 또는 링크 버튼으로 구현
  → 마커: 소망교회 (위도 37.5271, 경도 127.0268)

[네비게이션 버튼 3개]
  버튼을 누르면 앱에서 길 안내가 시작됩니다
  ┌─────────────┐  ┌──────────┐  ┌───────────┐
  │ 네이버 지도  │  │   T맵    │  │  카카오맵  │
  └─────────────┘  └──────────┘  └───────────┘

[구분선]

[자가용 안내 텍스트]
  신구중학교, 소망교회 본관 앞마당, 지하 주차장에 주차해주시기 바랍니다.

[지도 — 네이버 지도 (신구중학교)]
  → 마커: 신구중학교 (위도 37.5267, 경도 127.0262)

[네비게이션 버튼 3개 — 동일]
  ┌─────────────┐  ┌──────────┐  ┌───────────┐
  │ 네이버 지도  │  │   T맵    │  │  카카오맵  │
  └─────────────┘  └──────────┘  └───────────┘
```

#### 주소 정보

| 장소           | 도로명 주소                              | 좌표 (위도, 경도)          |
|----------------|------------------------------------------|----------------------------|
| 소망교회       | 서울특별시 강남구 압구정로36길 55        | 37.5271, 127.0268          |
| 신구중학교     | 서울특별시 강남구 압구정로36길 17        | 37.5267, 127.0262          |

#### 네비게이션 버튼 URL

목적지: **소망교회** / **신구중학교** 각각 별도 세트

| 앱        | 소망교회 URL                                                                 | 신구중학교 URL                                                               |
|-----------|------------------------------------------------------------------------------|------------------------------------------------------------------------------|
| 네이버 지도 | `https://map.naver.com/v5/directions/-/-/-/walk?c=37.5271,127.0268`        | `https://map.naver.com/v5/directions/-/-/-/walk?c=37.5267,127.0262`        |
| T맵       | `https://tmap.life/route?goalname=소망교회&goaly=37.5271&goalx=127.0268`   | `https://tmap.life/route?goalname=신구중학교&goaly=37.5267&goalx=127.0262` |
| 카카오맵  | `https://map.kakao.com/link/to/소망교회,37.5271,127.0268`                  | `https://map.kakao.com/link/to/신구중학교,37.5267,127.0262`                |

> 모바일에서 앱 미설치 시 웹 URL로 fallback. 앱 설치 시 자동으로 앱 실행.

---

## 6. 애니메이션

### 6.1 CSS 키프레임

| 이름        | 동작                                    |
|-------------|-----------------------------------------|
| `fadeDown`  | `opacity 0→1` + `translateY -20px→0`   |
| `fadeIn`    | `opacity 0→1`                           |

### 6.2 Scroll Reveal (`.reveal`)

`IntersectionObserver` threshold `0.15` — 요소가 15% 진입 시 `.visible` 클래스 추가.

```css
.reveal         { opacity: 0; transform: translateY(30px); transition: 0.8s ease; }
.reveal.visible { opacity: 1; transform: translateY(0); }
```

---

## 7. 파티클 효과 (`#particles`)

`<canvas>` 전체화면 고정 (`position: fixed`), `pointer-events: none`으로 인터랙션 차단.

| 속성          | 값                        |
|---------------|---------------------------|
| 파티클 수     | 80개                      |
| 색상          | `rgba(201,169,110, α)`    |
| 반지름        | 0.5 ~ 2.5px (랜덤)        |
| 이동 방향     | 위쪽 (y 감소)             |
| 흔들림        | `sin(wobble)` 좌우 wobble |
| 화면 이탈 시  | 하단에서 재생성            |

---

## 8. 추후 작업 (TODO)

- [ ] 모노그램 `A & B` → 실제 이름으로 교체
- [ ] RSVP 버튼 → 구글 폼 또는 별도 페이지 연결
- [ ] 우리의 이야기 버튼 → `#story` 앵커 또는 별도 페이지
- [ ] 네비게이션 버튼 좌표 최종 확인 (소망교회 / 신구중학교)
- [ ] Location 섹션 HTML 구현 (지도 + 버튼 3개 × 2세트)
- [ ] OG 메타태그 추가 (SNS 공유 미리보기용)
- [ ] 카운트다운 목표 시각 `2026-11-07T14:00:00` HTML 반영
